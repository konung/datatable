Next session:

2) individual column search
    - individual column

2.5) Rename the repo to be data_table

3) rails like config/initilizer for default settings of datatable
    DataTable.config do |config|
      config.default_options = {
        :bLengthChange => true
      }
    end

4) Theming
     jquery ui theme

5) web site, readme, wiki on github

6) initial development release

7) Working on parsing out column names from sql queries (parsing_spec.rb) (parsing things like select count(foo) as bar what type is bar?

-- column re-ordering
-- Find or write SQL pagination specs (Group pagination specs together)
-- sql version of helpers
-- sDom
-- like for integer columns?
-- always force use of @data table (in helper)

# Error checking:
What if I get a column name wrong in the columns hash?
