#) when a column is not searchable it should not render a text input
   for that columns search input

#) the last columns individual search paramter seems to be ignored


#) prorivde a reset link in so that all the search inputs are cleared
   would be nice if it was easily hooked to a keystroke for example
   ESC when any of the inputs have the focus

#) Provide example of fnRender

#) default sort orders

#) make it so both global search and individual column searches
   support typcasting so that those columns can be searched as
   strings, example:

       SELECT order_number FROM orders WHERE CAST(order_number as text) LIKE '%123%';

       column({'orders.order_number' => {:type => :string, :cast => :text }})

   having :cast present would force it to build any where clauses using a type cast

#) rails generator
     add javascript to public/javascripts
     add css for default datatable theme to public/stylesheets
     add template for config/initializer/datatable.rb
     create folder app/datatables/

#) web site, readme, wiki on github

#) release 0.1 (support for rails 3.0)

THE FUTURE
  Rails 3.1
  Searching Date Ranges
  Easy Printing of Table
  Download CSV/Excel
  Reordering Columns
  Lightbox Editing
  Column ReOrdering
  maybe parse the SQL ?
  get acitve releation version to do more
  it it possible to extract the where clause from the base sql reliably?

  #) Improve the initializer config code to be more robust
          https://github.com/jnicklas/carrierwave/blob/master/lib/carrierwave/uploader/configuration.rb


